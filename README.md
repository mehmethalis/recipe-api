# recipe-api

Basic rest api with Django

#### Setup

``docker-compose build .``

#### Start server

``docker-compose up ``

#### Run test

``docker-compose run --rm app sh -c "python manage.py test"``